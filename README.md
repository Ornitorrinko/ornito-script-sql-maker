# ornito-sql-script-maker

"Lightweight script maker based on a default template"

```sh
	Usage: node index.js --t 'procedure' or 'function' or 'view'
```

```h3
	Available templates: View, Stored procedures, Value-Scalar Functions
```